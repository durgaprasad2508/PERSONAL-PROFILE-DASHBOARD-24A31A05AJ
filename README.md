This is an educational demo. For production
use:
Replace flat-file storage with a proper database (SQLite / PostgreSQL / MySQL).
Add authentication & authorization.
Validate and sanitize file uploads.
Use environment variables (never commit secrets).
Serve static files via a CDN and enable HTTPS.
profile_data. json is included for quick demo/testing - it is not secure for multi-user apps.
